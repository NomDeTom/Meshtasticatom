# The place-and-radio matrix: Batumi, three presets, two scales, three placements, three archive
# counts, five seeds, with a no-archive baseline per seed per scale.
#
# Weekly rather than nightly. It is the heavier of the two sweeps - the link build is O(n^2) with a
# terrain profile and a clutter path walk per pair, so an x4 cell spends most of its time before the
# simulation starts - and it answers slower-moving questions than the mechanism sweep does: whether a
# preset ordering survives scaling, and whether any deliberate placement beats picking nodes at
# random.
#
# One job per (preset, scale, seed). Runs are independent processes with no shared state, so this
# parallelises perfectly; the only shared thing is the scenario cache, and each job gets its own.
#
# The seed used to be inside the job rather than on the matrix, and the runs were two simulated hours
# each. Both changed together, because neither works without the other: 2 h samples two hours of a
# 24-hour diurnal curve, so the cells were measuring one arbitrary stretch of one evening (see
# matrix.BASE), but at 72 h a whole cell of fifty runs in one sequential job comes to about five hours
# unmirrored and twenty mirrored - past this job's ceiling, and the mirrored figure past the
# platform's own 360-minute limit, which no `timeout-minutes` can raise. Sharding by seed cuts the job
# to ten runs and makes the duration affordable. It is also why matrix.py needed a `--tag`: fifty runs
# in one file needed no filename discipline, and five shards of ten do.
name: Sim Sweep (matrix)

on:
  schedule:
    # Daily at 03:40 UTC, two cells a day - so a digest lands every morning rather than once a week.
    # Clear of the nightly block sweep at 02:10 and the design rotation at 04:40.
    #
    # **Which two is decided by what the archive already holds, not by the date.** A date-keyed
    # rotation marches on regardless: a day that fails, or never fires, leaves its cells missing until
    # the cycle comes round again three days later, and nothing notices because nothing looks. Each
    # firing instead asks which cells have gone longest without a digest, so a failed part is retried
    # by the next run rather than skipped. See sfpp/nextup.py. Dispatch with `cells` to override.
    - cron: 40 3 * * *
  workflow_dispatch:
    inputs:
      seeds:
        description: Space-separated seeds. Five is the default; fewer for a quick look
        type: string
        required: false
        default: 7 11 13 17 23
      cells:
        description: Space-separated cell names from `python3 -m sfpp.matrix --list`. Empty rotates by
          date; `all` runs the whole grid at once
        type: string
        required: false
        default: ""

permissions: {}

concurrency:
  group: sim-sweep-matrix
  cancel-in-progress: false

jobs:
  plan:
    name: Draw the run
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      matrix: ${{ steps.draw.outputs.matrix }}
      cells: ${{ steps.draw.outputs.cells }}
      run_id: ${{ steps.draw.outputs.run_id }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Fetch the archive, so the plan can see what has already run
        # Shallow and read-only. Absent on the very first run of a fresh repository, which is not an
        # error - an empty archive simply means nothing has run and everything is equally stale.
        run: |
          set -euo pipefail
          mkdir -p archive
          if git fetch --depth 1 origin sim-results 2>/dev/null; then
            git --work-tree=archive checkout FETCH_HEAD -- runs 2>/dev/null || true
            echo "archive holds $(find archive -name summary.json | wc -l) digest(s)"
          else
            echo "no sim-results branch yet - treating every cell as never run"
          fi

      - name: Resolve the cell list
        id: draw
        env:
          IN_CELLS: ${{ inputs.cells }}
          IN_SEEDS: ${{ inputs.seeds }}
        run: |
          set -euo pipefail
          python3 - >> "$GITHUB_OUTPUT" <<'PY'
          import datetime, json, os, sys
          from sfpp.matrix import SHARDS_BY_MIRROR, cells

          known = cells()
          asked = (os.environ.get("IN_CELLS") or "").split()
          if asked == ["all"]:
              chosen = sorted(known)
          elif asked:
              unknown = [c for c in asked if c not in known]
              if unknown:
                  sys.exit(f"unknown cell(s): {', '.join(unknown)}")
              chosen = asked
          else:
              # The two cells that have gone longest without a digest - never run first, then oldest.
              # This is the check that makes the chain self-healing: a cell whose run failed still has
              # no digest, so the next firing picks it again instead of moving on.
              from sfpp.nextup import matrix_parts

              chosen = matrix_parts("archive/runs", 2)
              print(f"stalest cells: {' '.join(chosen)}", file=sys.stderr)

          # Seeds are fixed here rather than drawn. The matrix is a paired comparison across
          # presets and placements, and pairing is what makes it readable at five seeds instead of
          # fifty; a fresh draw each week would compare this week's mesh with last week's.
          seeds = (os.environ.get("IN_SEEDS") or "").split() or ["7", "11", "13", "17", "23"]
          bad = [s for s in seeds if not s.lstrip("-").isdigit()]
          if bad:
              sys.exit(f"seeds must be numbers, got {', '.join(repr(b) for b in bad)}")

          # One entry per (cell, seed, shard). Seeds were always safe to split - each is an
          # independent draw. The arms are safe now too, because placement draws from its own stream
          # (Campaign._place_servers), so every arm carries the control's offered load whichever job
          # it lands in. Measured: an x1 cell's ten arms at 72 h is 110 min on a runner, and x4 is
          # 3.15x that - 346 min, past this ceiling and near the platform's own 360-minute limit. Only
          # the x4 cells are split, and only as far as matrix.SHARDS_BY_MIRROR says.
          jobs = [
              {"cell": c, "seed": int(sd), "shard": f"{i}/{n}", "tag": f"s{sd}-{i}"}
              for c in chosen
              for sd in seeds
              for n in [SHARDS_BY_MIRROR.get(known[c][1], 1)]
              for i in range(n)
          ]
          if len(jobs) > 256:
              sys.exit(f"{len(jobs)} jobs exceeds the 256 a matrix may hold; run fewer cells or "
                       f"seeds at a time")

          print(f"matrix={json.dumps(jobs)}")
          # Cell names alone, never the shards: this is what the digest is checked against, and all
          # five shards of a cell carry the same `block` and collate as one.
          print(f"cells={' '.join(chosen)}")
          # UTC minute, not just the date. The blocks and design sweeps disambiguate a same-day
          # re-run with their seed base; this one has no seed base to use - its five seeds are fixed
          # so the cells stay paired - so a bare date meant two runs on one day wrote the same
          # directory and collate overwrote the first, with no merge and no warning. A rotation run
          # carries only its two stalest cells, so the survivor would not even hold the same ones.
          # Zero-padded, so a name still sorts chronologically.
          stamp = datetime.datetime.now(datetime.timezone.utc)
          print(f"run_id=matrix-{stamp:%Y-%m-%d-%H%M}")
          PY

  matrix:
    name: ${{ matrix.cell }} @${{ matrix.seed }} ${{ matrix.shard }}
    needs: plan
    runs-on: ubuntu-latest
    permissions:
      contents: read
    # Measured on a real runner rather than extrapolated: an x1 cell's ten arms at 72 h took 110
    # minutes (660 s a run - 1.9x this tree's own container). x4 is 3.15x per run, so its arms are
    # split three ways and every job is now ~110-120 minutes whatever the scale. 240 leaves room for a
    # runner half this speed and stays well clear of the platform's 360-minute hard limit.
    timeout-minutes: 240
    strategy:
      fail-fast: false
      max-parallel: 6
      matrix:
        include: ${{ fromJSON(needs.plan.outputs.matrix) }}
    env:
      # `--mirror` writes a mirrored clutter CSV into the cache. Two jobs racing to write the same
      # one is worth avoiding, and a per-job cache costs nothing but a rebuild - and with the cells
      # sharded by seed there are now five jobs per cell, so the key has to carry the seed too.
      SFPP_SCENARIO_CACHE: ${{ github.workspace }}/.cache/${{ matrix.cell }}-${{ matrix.tag }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # Required here, not optional: every cell of this matrix is --scenario batumi, which reads
      # presets/batumi.yaml.
      - name: Install the scenario loader
        run: pip install --quiet "PyYAML>=6.0"

      - name: ${{ matrix.cell }} at seed ${{ matrix.seed }} shard ${{ matrix.shard }}
        env:
          CELL: ${{ matrix.cell }}
          SEED: ${{ matrix.seed }}
          SHARD: ${{ matrix.shard }}
          TAG: ${{ matrix.tag }}
        run: |
          set -euo pipefail
          # --tag names the file apart from the cell's other seeds and shards. The reports keep the
          # cell's name, so the digest reads them all as one block. Without it every shard writes
          # {cell}.json and the collate job's `merge-multiple: true` download silently keeps whichever
          # arrived last.
          python3 -u -m sfpp.matrix --cell "$CELL" --seeds "$SEED" --shard "$SHARD"             --tag "$TAG" --out out

      - uses: actions/upload-artifact@v4
        if: always() # a shard that failed halfway still wrote the configurations it finished
        with:
          name: matrix-${{ matrix.cell }}-${{ matrix.tag }}
          path: out
          if-no-files-found: warn
          retention-days: 90

  collate:
    name: Collate and publish
    needs: [plan, matrix]
    if: ${{ !cancelled() }}
    uses: ./.github/workflows/sim_collate.yml
    permissions:
      contents: write
    with:
      run_id: ${{ needs.plan.outputs.run_id }}
      scenario: batumi
      expect: ${{ needs.plan.outputs.cells }}
      artifact_pattern: matrix-*
